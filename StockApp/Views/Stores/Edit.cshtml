@model Stock.BL.Stores.DTOs.StoreUpdateDTO

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-content collapse show">
                <div class="card-body card-dashboard">
                    <form asp-action="Edit" method="post" asp-controller="Stores">
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="row">
                            <input asp-for="Id" hidden/>
                            <div class="form-group col-6">
                                <label asp-for="Name" class="control-label">Store Name:</label>
                                <input asp-for="Name" class="form-control" required />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>
                            <div class="form-group col-6">
                                <label asp-for="Address" class="control-label">Address:</label>
                                <input asp-for="Address" class="form-control" />
                            </div>
                            <div class="form-group col-6">
                                <label asp-for="MobileNumber" class="control-label">Mobile Number:</label>
                                <input asp-for="MobileNumber" class="form-control">
                            </div>
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Save" class="btn btn-primary col-lg-4 col-md-6 col-sm-12" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}


